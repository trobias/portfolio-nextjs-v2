# Claude project context

@AGENTS.md

Start with `SPEC.md`, then follow the product, design, evidence, privacy and verification rules imported above. Continue from `tasks/todo.md`; do not rebuild completed baseline work. Durable facts belong in the referenced project documents, not in ad-hoc prompts.

The project archive is intentionally organized by the four `group` values defined in `lib/projects.ts`; preserve those chapters, stable anchors and compact responsive grid when editing project content.

Primary pill CTAs use `AnimatedPillLink`; do not replace its directional fill sweep with a generic background-color hover.

This is the independent Next.js repository `trobias/portfolio-nextjs-v2`, deployed by the separate Vercel project `portfolio-nextjs-v2` at `portfolio-nextjs-v2-ashen.vercel.app`; never push it to the legacy `trobias/PORTFOLIO` repository.
